# expo-router ships optional web-only deps (vaul, @radix-ui/*) whose peer ranges don't line up
# with the React 19 version this app actually installs; since we don't use Expo Router's web
# target, that peer conflict is harmless but npm still hard-fails on it without this.
legacy-peer-deps=true
